<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= subject %></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />
    <style>
        body {
            margin: 0;
            padding: 0;
            background-color: #0d0f16;
            font-family: "Inter", "Segoe UI", Arial, sans-serif;
            color: #e5e7eb;
        }

        .email-container {
            max-width: 650px;
            margin: 32px auto;
            background-color: #151821;
            border-radius: 14px;
            overflow: hidden;
        }

        .header {
            background: linear-gradient(90deg, #fcd45b, #fce16e);
            color: #0d0f16;
            text-align: center;
            padding: 26px 20px;
        }

        .header h1 {
            margin: 0;
            font-size: 24px;
            font-weight: 700;
            letter-spacing: 0.4px;
        }

        .content {
            padding: 32px 36px 36px;
        }

        .greeting {
            font-size: 18px;
            font-weight: 600;
            margin: 0 0 6px;
            color: #ffffff;
        }

        .subtext {
            font-size: 14.5px;
            color: #cbd5e1;
            margin: 0 0 20px;
        }

        .project-info {
            background: #1c1f29;
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 24px;
            border: 1px solid #2d313d;
        }

        .project-name {
            font-size: 18px;
            font-weight: 600;
            color: #fcd45b;
            margin-bottom: 8px;
        }

        .event-count {
            font-size: 14px;
            color: #9ca3af;
            margin-bottom: 16px;
        }

        .events-list {
            display: flex;
            flex-direction: column;
            gap: 16px;
        }

        .event-item {
            background: #11131a;
            border-radius: 8px;
            padding: 20px;
            border: 1px solid #2d313d;
        }

        .event-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 16px;
            padding-bottom: 12px;
            border-bottom: 1px solid #2d313d;
        }

        .event-title {
            font-size: 16px;
            font-weight: 600;
            color: #ffffff;
        }

        .event-date {
            font-size: 14px;
            color: #fcd45b;
            font-weight: 500;
        }

        .event-details {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 16px;
        }

        .detail-item {
            display: flex;
            align-items: flex-start;
            gap: 10px;
        }

        .detail-icon {
            color: #9ca3af;
            min-width: 18px;
        }

        .detail-label {
            font-size: 11px;
            color: #9ca3af;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 2px;
        }

        .detail-value {
            font-size: 14px;
            color: #e5e7eb;
        }

        .footer {
            margin-top: 26px;
            font-size: 14px;
            color: #cbd5e1;
        }

        .email-footer {
            background: #11131a;
            text-align: center;
            padding: 22px 16px;
            font-size: 13px;
            color: #9ca3af;
            border-top: 1px solid #2d313d;
        }

        @media (max-width: 600px) {
            .content {
                padding: 24px;
            }
            
            .event-details {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="email-container">
        <!-- Header -->
        <div class="header">
            <h1>New Event Assignments</h1>
        </div>

        <!-- Content -->
        <div class="content">
            <p class="greeting">Hello <%= memberName %>,</p>
            <p class="subtext">
                You have been assigned to events in the following project:
            </p>

            <!-- Project Info -->
            <div class="project-info">
                <div class="project-name"><%= projectName %></div>
                <div class="event-count">Total events assigned: <%= events.length %></div>
            </div>

            <!-- Events List -->
            <div class="events-list">
                <% events.forEach(function(event) { %>
                <div class="event-item">
                    <div class="event-header">
                        <div class="event-title"><%= event.eventName %></div>
                        <div class="event-date"><%= event.eventDate %></div>
                    </div>
                    
                    <div class="event-details">
                        <!-- Time -->
                        <div class="detail-item">
                            <div class="detail-icon">⏰</div>
                            <div>
                                <div class="detail-label">Time</div>
                                <div class="detail-value"><%= event.startHour %> – <%= event.endHour %></div>
                            </div>
                        </div>

                        <!-- Location -->
                        <div class="detail-item">
                            <div class="detail-icon">📍</div>
                            <div>
                                <div class="detail-label">Location</div>
                                <div class="detail-value"><%= event.location %></div>
                            </div>
                        </div>

                        <!-- Organized By -->
                        <div class="detail-item">
                            <div class="detail-icon">🏢</div>
                            <div>
                                <div class="detail-label">Organized by</div>
                                <div class="detail-value"><%= companyName %></div>
                            </div>
                        </div>
                    </div>
                </div>
                <% }); %>
            </div>

            <p class="footer">
                Please review the event details above. All times are in your local timezone.
            </p>
        </div>

        <!-- Footer -->
        <div class="email-footer">
            <p>This is an automated notification from <%= companyName %>.</p>
            <p>If you have any questions, please contact your Project Manager or Admin.</p>
        </div>
    </div>
</body>
</html>